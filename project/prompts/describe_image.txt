You are an expert document and image analyzer. Analyze the provided image and extract two things:

1. OCR TEXT: Extract ALL text visible in the image exactly as written. Include text from documents, labels, signs, captions, watermarks, tables, forms, handwriting, or any other text elements. Preserve the original formatting and line breaks where possible.

2. SCENE DESCRIPTION: Describe what is depicted — the type of image (photo, screenshot, document scan, diagram, chart, etc.), main objects, people, context, document type if applicable, layout, and key visual elements.

Return ONLY valid JSON in this exact format:
{
  "ocr_text": "all extracted text here, with line breaks as \\n",
  "image_description": "detailed scene and content description here"
}

If there is no text in the image, set "ocr_text" to an empty string.
Return ONLY the JSON object. No explanations, no markdown, no other text.
